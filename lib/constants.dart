import 'package:flutter/material.dart';

const primaryColor = Color(0xFF2697FF);
const secondaryColor = Color(0xFF2A2D3E);
const bgColor = Color(0xFF212332);
const bgColorMain = Color.fromRGBO(15, 23, 42, 1.0);
const textColor = Color.fromRGBO(148, 163, 184, 1);
const defaultPadding = 16.0;
const mainColor = Color.fromRGBO(94, 234, 212, 1);
const dividerLine = Divider(
  color: mainColor,
  height: 25,
  thickness: 2,
  endIndent: 5,
);

const String path = "assets/icons/";

const defaultPaddingBox = SizedBox(height: 20);


